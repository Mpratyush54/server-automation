apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: caps-service
spec:
  rules:
  - host: caps-service.internal
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: caps-service
            port:
              number: 3000
