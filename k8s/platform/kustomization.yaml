# Minimal kustomize for Argo CD Image Updater write-back.
# Runtime installs still come from platformctl; this tracks image tags for auto-update.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - api-image.yaml
  - portal-image.yaml
