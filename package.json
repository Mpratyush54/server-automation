{
  "name": "platform-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Platform - Internal PaaS",
  "scripts": {
    "install:all": "cd platform/api && npm install && cd ../portal && npm install && cd ../../sdk-node && npm install",
    "build:api": "cd platform/api && npm run build",
    "build:portal": "cd platform/portal && npm run build",
    "build:sdk": "cd sdk-node && npm run build",
    "build:all": "npm run build:api && npm run build:portal && npm run build:sdk",
    "start:api": "cd platform/api && npm start",
    "start:api:dev": "cd platform/api && npm run dev",
    "start:portal": "cd platform/portal && npx ng serve",
    "start:portal:prod": "cd platform/portal && npx ng serve --proxy-config proxy.conf.json",
    "start": "concurrently \"npm run start:api:dev\" \"npm run start:portal\"",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:build": "docker-compose build"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
